name: "Build Site"
description: "Install frameworks and dependencies, then build the site."
author: "bmurty"
runs:
  using: composite
  steps:
    - name: Install Deno
      uses: denoland/setup-deno@v2
      with:
        deno-version: v2.x
    - name: Setup environment
      shell: bash
      run: deno task setup
    - name: Install exiftool
      shell: bash
      run: sudo apt-get -y install exiftool
    - name: Build site
      shell: bash
      run: deno task build
    - name: Save site build files to artifacts
      uses: actions/upload-artifact@v4
      with:
        name: site-build
        overwrite: true
        include-hidden-files: true
        retention-days: 1
        compression-level: 9
        path: |
          build/
          public/
          .env
branding:
  icon: "box"
  color: "green"
